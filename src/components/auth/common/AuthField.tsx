interface AuthFieldProps extends React.InputHTMLAttributes<HTMLInputElement> { label: string; error?: string; }
export function AuthField({ label, error, id, ...inputProps }: AuthFieldProps) { const errorId = error ? `${id}-error` : undefined; return <div className="space-y-1.5"><label htmlFor={id} className="block text-xs font-semibold text-text-secondary">{label}</label><input id={id} aria-invalid={Boolean(error)} aria-describedby={errorId} className="input-base w-full" {...inputProps} />{error && <p id={errorId} className="text-xs text-accent-danger" role="alert">{error}</p>}</div>; }
